# Customer-Churn-Predictor-Model
An independent project on predictive analytics for telecom customer retention. It is prepared with an end-to-end ML pipeline: data preprocessing, SMOTE resampling, and Logistic Regression. The project identifies key churn drivers like service call frequency and international plan status.
